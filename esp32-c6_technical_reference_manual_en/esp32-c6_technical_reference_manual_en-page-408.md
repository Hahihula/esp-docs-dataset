

```markdown
Chapter 11 Event Task Matrix (SOC_ETM)   GoBack

5. When channeln no longer needs to map the selected event to the selected task, disable channeln by setting SOC_ETM_CH_DISABLEn. To configure a new event and task mapping, repeat Steps 1 to 3. If no configurations, channeln will remain disabled.

6. The ETM module can be reset by writing 1 and then 0 to the PCR_ETM_RST_EN field
```