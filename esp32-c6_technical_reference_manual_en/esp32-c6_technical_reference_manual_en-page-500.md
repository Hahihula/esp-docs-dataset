

```markdown
Chapter 13 System Timer (SYSTIMER) GoBack

13.4.4 Synchronization Operation

The clock (APB_CLK) used in software operation is separate from CNT_CLK to drive the timer counters and comparators. Synchronization is needed for some configuration registers. A complete synchronization action takes two steps:
```