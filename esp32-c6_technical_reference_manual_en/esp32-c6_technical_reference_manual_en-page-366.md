

```markdown
Register 8.76. LP_CLKRST_LP_CLK_PO_EN_REG (0x0004)

LP_CLKRST_AON_SLOW_OEN   Configures the clock gate to pad of the LP_DYN_SLOW_CLK.
    0: Disable the clk pass clock gate
    1: Enable the clk pass clock gate
    (R/W)

LP_CLKRST_AON_FAST_OEN   Configures the clock gate to pad of the LP_DYN_FAST_CLK.
    0: Disable the clk pass clock gate
    1: Enable the clk pass clock gate
    (R/W)

LP_CLKRST_SOSC_OEN       Configures the clock gate to pad of the OSC_SLOW_CLK.
    0: Disable the clk pass clock gate
    1: Enable the clk pass clock gate
    (R/W)

LP_CLKRST_FOSC_OEN       Configures the clock gate to pad of the RC_FAST_CLK.
    0: Disable the clk pass clock gate
    1: Enable the clk pass clock gate
    (R/W)

LP_CLKRST_XTAL32K_OEN    Configures the clock gate to pad of the XTAL32K_CLK.
    0: Disable the clk pass clock gate
    1: Enable the clk pass clock gate
    (R/W)
```