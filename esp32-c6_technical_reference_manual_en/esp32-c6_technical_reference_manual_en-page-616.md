

# 18.6 Registers

The addresses of **bus logging configuration registers** (see 18.6.1) in this section are relative to `MEM_MONITOR` base address. The addresses of other registers (see 18.6.2) are relative to the `ASSIST_DEBUG` base address. Both base addresses are provided in Table 5.3-2 in Chapter 5 *System and Memory*.