**Title:**
Chapter 12 DPort Registers

**Link:**
GoBack

**Register Information:**
- **Register Name:** DPORt_AHB MPU_TABLE_1_REG (0x0B8)
- **Bit Description Diagram:**
  - The diagram shows a bit map with labels for each position.
  - Bits are labeled from right to left as follows:
    - `31` is the most significant bit on the far left, followed by other bits in decreasing order down to `0`.
  - There's an indication of "reserved" between certain positions.

**Bit Description:**
- **DPORt_AHB_ACCESS_GRANT_1:** MPU for DMA. (R/W)

**Footer Information:**
- Page number and document version:
  - Espressif Systems
  - ESP32 TRM (Version 5.6)
- Link to submit feedback on documentation.

The diagram visually represents the bit layout of a specific register, indicating which bits are reserved or used for particular purposes such as granting access in DMA operations within an MPU context.