**Chapter Title:**
Chapter 26 SDIO Slave Controller (SDIO)

**Section Header:**
Register 26.31. SLCHOST_CONF_W15_REG (0xB8)

**Diagram Description:**
A diagram showing the layout of a register with labels for different bits:
- Bits are labeled as follows from left to right and top to bottom in rows, columns respectively.
- The first row has bit positions 24 through 31 starting at "SLCHOST_CONF63" on the far left. 
- Each subsequent line shows decreasing position numbers (downwards) with corresponding labels like SLCHOST_CONF60.

**Text Descriptions:**
- **SLCHOST_CONF63**: The information interaction register between Host and Slave. Both Host and Slave can access it. (R/W)
- **SLCHOST_CONF62**: The information interaction register between Host and Slave. Both Host and Slave can access it. (R/W)
- **SLCHOST_CONF61**: The information interaction register between Host and Slave. Both Host and Slave can access it. (R/W)
- **SLCHOST_CONF60**: The information interaction register between Host and Slave. Both Host and Slave can access it. (R/W)

**Footer:**
Espressif Systems
Submit Documentation Feedback

**Page Number:** 592 ESP32 TRM (Version 5.6)