**Title:**
Chapter 28 LED PWM Controller (LEDC)

**GoBack**

---

**Figure Title and Diagrams:**
- Figure 28.2-1 to -4 are not described in text, but they appear as diagrams related to the content.

**Section Heading:**
28.2.2 Timers

**Subsection with Image Caption (No image description provided):**
- **Caption:** LED_PWM High-speed Channel Diagram
- Figure 28.2-1 is referenced here without a detailed caption in text, but it appears as an illustration related to the timers section.

---

**Section Heading:**
28.2.3 LED_PWM Divider

**Body Text with Explanation of Equation and Concepts (No code blocks or tables):**

The clock of each high-speed timer, LEDC_CLKx, has two clock sources: REF_TICK or APB_CLK. For more information on the clock sources, please see Chapter Reset And Clock. The input clock is divided down by a divider first. The division factor is specified by LEDC_CLK_DIV_NUM_HSTIMERx which contains a fixed point number; the highest 10 bits represent the integer portion A, while the lowest eight bits contain the fractional portion B. The effective division factor LEDC_CLK_DIVx is as follows:

\[ \text{LEDC_CLK_DIVx} = \frac{A + \frac{B}{256}}{} \]

The division factor ranges from 1 ~ 1023.

When the fractional part B is not zero, the input and output clock of the divider is shown in figure 28.2-3.
Among the 256 output clocks, B are divided by (A+1), whereas the remaining (256-B) are divided by A. Output clocks divided by (A+1) are evenly distributed in the total 256 output clocks.

The output clock of the divider is the base clock for the counter which will count up to the value specified in LEDC_HSTIMERx_DUTY_RES. An overflow interrupt will be generated once the counting value reaches \( \text{LEDClus}_\text{HSTIMERx}_\text{DUTY_RES} = 1 \), at which point the counter restarts counting from O. It is also possible to reset, suspend, and read the values of the counter by software.

The output signal of the timer is the 20-bit value generated by the counter. The cycle period of this signal defines the frequency of the signals of any PWM channels connected to this timer.
The frequency of a PWM generator output signal, sig_outn, depends on the frequency of the timer’s clock source LEDC_CLKx; the division factor of the divider LEDC_CLK_DIVx, as well as the duty resolution (counter.

**Footer:**
Espressif Systems
630 ESP32 TRM (Version 5.6)
Submit Documentation Feedback