**Chapter Title:**
Chapter 22 I2S Controller (I2S)

**Section Header:**
Register 22.6. I2S_INT_ENA_REG (0x0014)

**Diagram Description:**
The diagram shows a register map with various interrupt enable bits for different events in the I2S controller.

**Interrupt Enable Bits Descriptions and Details:**

- **I2S_OUT_TOTAL_EOF_INT_ENA**: The interrupt enable bit for the `I2S_OUT_TOTAL_EOF_INT` interrupt.
  - Access Type (R/W)

- **I2S_IN_DSCR_EMPTY_INT_ENA**: The interrupt enable bit for the `I2S_IN_DSCR_EMPTY_INT` interrupt.
  - Access Type (R/W)

- **I2S_OUT_DSCR_ERR_INT_ENA**: The interrupt enable bit for the `I2S_OUT_DSCR_ERR_INT` interrupt.
  - Access Type (R/W)

- **I2S_IN_DSCR_ERR_INT_ENA**: The interrupt enable bit for the `I2S_IN_DSCR_ERR_INT` interrupt.
  - Access Type (R/W)

- **I2S_OUT_EOF_INT_ENA**: The interrupt enable bit for the `I2S_OUT_EOF_INT` interrupt.
  - Access Type (R/W)

- **I2S_OUT_DONE_INT_ENA**: The interrupt enable bit for the `I2S_OUT_DONE_INT` interrupt.
  - Access Type (R/W)

- **I2S_IN_SUC_EOF_INT_ENA**: The interrupt enable bit for the `I2S_IN_SUC_EOF_INT` interrupt.
  - Access Type (R/W)

- **I2S_IN_DONE_INT_ENA**: The interrupt enable bit for the `I2S_IN_DONE_INT` interrupt.
  - Access Type (R/W)

- **I2S_TX_HUNG_INT_ENA**: The interrupt enable bit for the `I2S_TX_HUNG_INT` interrupt.
  - Access Type (R/W)

- **I2S_RX_HUNG_INT_ENA**: The interrupt enable bit for the `I2S_RX_HUNG_INT` interrupt.
  - Access Type (R/W)

- **I2S_TX_REMPTY_INT_ENA**: The interrupt enable bit for the `I2S_TX_REMPTY_INT` interrupt.
  - Access Type (R/W)

- **I2S_TX_WFULL_INT_ENA**: The interrupt enable bit for the `I2S_TX_WFULL_INT` interrupt.
  - Access Type (R/W)

- **I2S_RX_REMPTY_INT_ENA**: The interrupt enable bit for the `I2S_RX_REMPTY_INT` interrupt.
  - Access Type (R/W)

- **I2S_RX_WFULL_INT_ENA**: The interrupt enable bit for the `I2S_RX_WFULINT` interrupt.
  - Access Type (R/W)

- **I2S_TX_PUT_DATA_INT_ENA**: The interrupt enable bit for the `I2S_TX_PUT_DATA_INT` interrupt.
  - Access Type (R/W)

- **I2S_RX_TAKE_DATA_INT_ENA**: The interrupt enable bit for the `I2S_RX_TAKE_DATA_INT` interrupt.
  - Access Type (R/W)

**Footer:**
Espressif Systems
Page Number: 436
Document Version: ESP32 TRM (Version 5.6)
Feedback Link Texts:
- Submit Documentation Feedback