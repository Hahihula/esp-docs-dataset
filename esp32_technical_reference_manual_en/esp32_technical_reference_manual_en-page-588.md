**Chapter Title:**
Chapter 26 SDIO Slave Controller (SDIO)

**Section Header:**
Register 26.23. SLCHOST_CONF_W6_REG (0x88)

**Diagram Description for Register 26.23:**
- The diagram shows a register with bits labeled from left to right as follows:
  - Bit positions are numbered sequentially.
  - Each bit position is associated with the label "SLCHOST_CONF27" through "SLCHOST_CONF24".
  - There's an indication of reset value (0x000) for each set of consecutive bits.

**Text Description:**
- **SLCHOST_CONF27**: The information interaction register between Host and Slave. Both Host and Slave can access it. (R/W)
- **SLCHOST_CONF26**: The information interaction register between Host and Slave. Both Host and Slave can access it. (R/W)

**Section Header:**
Register 26.24. SLCHOST_CONF_W8_REG (0x9C)

**Diagram Description for Register 26.24:**
- Similar to the previous diagram, this one shows a register with bits labeled from left to right as follows:
  - Bit positions are numbered sequentially.
  - Each bit position is associated with the label "SLCHOST_CONF35" through "SLCHOST_CONF32".
  - There's an indication of reset value (0x000) for each set of consecutive bits.

**Text Description:**
- **SLCHOST_CONF35**: The information interaction register between Host and Slave. Both Host and Slave can access it. (R/W)
- **SLCHOST_CONF34**: The information interaction register between Host and Slave. Both Host and Slave can access it. (R/W)

**Footer Information:**
Espressif Systems
Submit Documentation Feedback

**Page Numbering:**
588 ESP32 TRM (Version 5.6)