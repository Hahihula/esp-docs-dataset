---
original_file_path: api-reference/peripherals/etm.rst
---

# Event Task Matrix (ETM)

`zh_CN:[中文]`{.interpreted-text role="link_to_translation"}

## Introduction

Normally, if a peripheral X needs to notify peripheral Y of a particular event, this could only be done via a CPU interrupt from peripheral X, where the CPU notifies peripheral Y on behalf of peripheral X. However, in time-critical applications, the latency introduced by CPU interrupts is non-negligible.

With the help of the Event Task Matrix (ETM) module, some peripherals can directly notify other peripherals of events through pre-set connections without the intervention of CPU interrupts. This allows precise and low latency synchronization between peripherals, and lessens the CPU\'s workload as the CPU no longer needs to handle these events.

::: {.blockdiag caption="ETM channels Overview" align="center"}
/../\_static/diagrams/etm/etm_channel.diag
:::

The ETM module has multiple programmable channels, they are used to connect a particular **Event** to a particular **Task**. When an event is activated, the ETM channel will trigger the corresponding task automatically.

Peripherals that support ETM functionality provide their or unique set of events and tasks to be connected by the ETM. An ETM channel can connect any event to any task, even looping back an event to a task on the same peripheral. However, an ETM channel can only connect one event to one task at a time (i.e., 1 to 1 relation). If you want to use different events to trigger the same task, you can set up more ETM channels.

Typically, with the help of the ETM module, you can implement features like:

- Toggle the GPIO when a timer alarm event happens
- Start an ADC conversion when a pulse edge is detected on a GPIO

## Functional Overview

The following sections of this document cover the typical steps to configure and use the ETM module.

- `etm-channel-allocation`{.interpreted-text role="ref"} - describes how to install and uninstall the ETM channel.
- `etm-event`{.interpreted-text role="ref"} - describes how to allocate a new ETM event handle or fetch an existing handle from various peripherals.
- `etm-task`{.interpreted-text role="ref"} - describes how to allocate a new ETM task handle or fetch an existing handle from various peripherals.
- `etm-channel-control`{.interpreted-text role="ref"} - describes common ETM channel control functions.
- `etm-power-management`{.interpreted-text role="ref"} - describes the options and strategies provided by the driver in order to save power.
- `etm-thread-safety`{.interpreted-text role="ref"} - lists which APIs are guaranteed to be thread-safe by the driver.
- `etm-kconfig-options`{.interpreted-text role="ref"} - lists the supported Kconfig options that can be used to make a different effect on driver behavior.

### ETM Channel Allocation

There are many identical ETM channels in {IDF_TARGET_NAME}[^1], and each channel is represented by `esp_etm_channel_handle_t`{.interpreted-text role="cpp:type"} in the software. The ETM core driver manages all available hardware resources in a pool so that you do not need to care about which channel is in use and which is not. The ETM core driver will allocate a channel for you when you call `esp_etm_new_channel`{.interpreted-text role="cpp:func"} and delete it when you call `esp_etm_del_channel`{.interpreted-text role="cpp:func"}. All requirements needed for allocating a channel are provided in `esp_etm_channel_config_t`{.interpreted-text role="cpp:type"}.

Before deleting an ETM channel, please disable it by `esp_etm_channel_disable`{.interpreted-text role="cpp:func"} in advance or make sure it has not been enabled yet by `esp_etm_channel_enable`{.interpreted-text role="cpp:func"}.

### ETM Event

ETM Event abstracts the event source, masking the details of specific event sources, and is represented by `esp_etm_event_handle_t`{.interpreted-text role="cpp:type"} in the software, allowing applications to handle different types of events more easily. ETM events can be generated from a variety of peripherals, thus the way to get the event handle differs from peripherals. When an ETM event is no longer used, you should call `esp_etm_channel_connect`{.interpreted-text role="cpp:func"} with a `NULL` event handle to disconnect it and then call `esp_etm_del_event`{.interpreted-text role="cpp:func"} to free the event resource.

#### GPIO Events

GPIO **edge** event is the most common event type, it can be generated by any GPIO pin. You can call `gpio_new_etm_event`{.interpreted-text role="cpp:func"} to create a GPIO event handle, with the configurations provided in `gpio_etm_event_config_t`{.interpreted-text role="cpp:type"}:

- `gpio_etm_event_config_t::edge`{.interpreted-text role="cpp:member"} or `gpio_etm_event_config_t::edges`{.interpreted-text role="cpp:member"} decides which edge(s) to trigger the event(s), supported edge types are listed in the `gpio_etm_event_edge_t`{.interpreted-text role="cpp:type"}.

You need to build a connection between the GPIO ETM event handle and the GPIO number. So you should call `gpio_etm_event_bind_gpio`{.interpreted-text role="cpp:func"} afterwards. Please note, only the ETM event handle that created by `gpio_new_etm_event`{.interpreted-text role="cpp:func"} can set a GPIO number. Calling this function with other kinds of ETM events returns `ESP_ERR_INVALID_ARG`{.interpreted-text role="c:macro"} error. Needless to say, this function does not help with the GPIO initialization, you still need to call `gpio_config`{.interpreted-text role="cpp:func"} to set the property like direction, pull up/down mode separately.

#### Other Peripheral Events

::: list

SOC_SYSTIMER_SUPPORT_ETM

:   - You can call `esp_systick_new_etm_alarm_event`{.interpreted-text role="cpp:func"} to get the ETM event from RTOS Systick, one per CPU core.

SOC_SYSTIMER_SUPPORT_ETM

:   - Refer to `/api-reference/system/esp_timer`{.interpreted-text role="doc"} for how to get the ETM event handle from esp_timer.

SOC_TIMER_SUPPORT_ETM

:   - Refer to `gptimer-etm-event-and-task`{.interpreted-text role="ref"} for how to get the ETM event handle from GPTimer.

SOC_GDMA_SUPPORT_ETM

:   - Refer to `/api-reference/system/async_memcpy`{.interpreted-text role="doc"} for how to get the ETM event handle from async memcpy.

SOC_MCPWM_SUPPORT_ETM

:   - Refer to `/api-reference/peripherals/mcpwm`{.interpreted-text role="doc"} for how to get the ETM event handle from MCPWM.

SOC_ANA_CMPR_SUPPORT_ETM

:   - Refer to `/api-reference/peripherals/ana_cmpr`{.interpreted-text role="doc"} for how to get the ETM event handle from analog comparator.

SOC_TEMPERATURE_SENSOR_SUPPORT_ETM

:   - Refer to `/api-reference/peripherals/temp_sensor`{.interpreted-text role="doc"} for how to get the ETM event handle from temperature sensor.

SOC_I2S_SUPPORTS_ETM

:   - Refer to `/api-reference/peripherals/i2s`{.interpreted-text role="doc"} for how to get the ETM event handle from I2S.

SOC_LEDC_SUPPORT_ETM

:   - Refer to `/api-reference/peripherals/ledc`{.interpreted-text role="doc"} for how to get the ETM event handle from LEDC.
:::

### ETM Task

ETM Task abstracts the task action and is represented by `esp_etm_task_handle_t`{.interpreted-text role="cpp:type"} in the software, allowing tasks to be managed and represented in the same way. ETM tasks can be assigned to a variety of peripherals, thus the way to get the task handle differs from peripherals. When an ETM task is no longer used, you should call `esp_etm_channel_connect`{.interpreted-text role="cpp:func"} with a `NULL` task handle to disconnect it and then call `esp_etm_del_task`{.interpreted-text role="cpp:func"} to free the task resource.

#### GPIO Tasks

GPIO task is the most common task type. One GPIO can take one or more GPIO ETM task actions, and one GPIO ETM task action can even manage multiple GPIOs. When the task gets activated by the ETM channel, all managed GPIOs can set/clear/toggle at the same time. You can call `gpio_new_etm_task`{.interpreted-text role="cpp:func"} to create a GPIO task handle, with the configurations provided in `gpio_etm_task_config_t`{.interpreted-text role="cpp:type"}:

- `gpio_etm_task_config_t::action`{.interpreted-text role="cpp:member"} or `gpio_etm_task_config_t::actions`{.interpreted-text role="cpp:member"} decides what GPIO action(s) would be taken by the ETM task. Supported actions are listed in the `gpio_etm_task_action_t`{.interpreted-text role="cpp:type"}. If one GPIO needs to take more than one actions, the action tasks have to be created in one `gpio_new_etm_task`{.interpreted-text role="cpp:func"} call with filling the actions into the array of `gpio_etm_task_config_t::actions`{.interpreted-text role="cpp:member"}.

To build a connection between the GPIO ETM task and the GPIO number, you should call `gpio_etm_task_add_gpio`{.interpreted-text role="cpp:func"}. You can call this function by several times if you want the task handle to manage more GPIOs. Please note, only the ETM task handle that created by `gpio_new_etm_task`{.interpreted-text role="cpp:func"} can manage a GPIO. Calling this function with other kinds of ETM tasks returns `ESP_ERR_INVALID_ARG`{.interpreted-text role="c:macro"} error. Needless to say, this function does not help with the GPIO initialization, you still need to call `gpio_config`{.interpreted-text role="cpp:func"} to set the property like direction, pull up/down mode separately.

Before you call `esp_etm_del_task`{.interpreted-text role="cpp:func"} to delete the GPIO ETM task, make sure that all previously added GPIOs are removed by `gpio_etm_task_rm_gpio`{.interpreted-text role="cpp:func"} in advance.

#### Other Peripheral Tasks

::: list

SOC_TIMER_SUPPORT_ETM

:   - Refer to `gptimer-etm-event-and-task`{.interpreted-text role="ref"} for how to get the ETM task handle from GPTimer.

SOC_TEMPERATURE_SENSOR_SUPPORT_ETM

:   - Refer to `/api-reference/peripherals/temp_sensor`{.interpreted-text role="doc"} for how to get the ETM task handle from temperature sensor.

SOC_I2S_SUPPORTS_ETM

:   - Refer to `/api-reference/peripherals/i2s`{.interpreted-text role="doc"} for how to get the ETM task handle from I2S.

SOC_LEDC_SUPPORT_ETM

:   - Refer to `/api-reference/peripherals/ledc`{.interpreted-text role="doc"} for how to get the ETM task handle from LEDC.
:::

### ETM Channel Control

#### Connect Event and Task

An ETM event has no association with an ETM task, until they are connected to the same ETM channel by calling `esp_etm_channel_connect`{.interpreted-text role="cpp:func"}. Especially, calling the function with a `NULL` task/event handle means disconnecting the channel from any task or event. Note that, this function can be called either before or after the channel is enabled. But calling this function at runtime to change the connection can be dangerous, because the channel may be in the middle of a cycle, and the new connection may not take effect immediately.

#### Enable and Disable Channel

You can call `esp_etm_channel_enable`{.interpreted-text role="cpp:func"} and `esp_etm_channel_disable`{.interpreted-text role="cpp:func"} to enable and disable the ETM channel from working.

#### ETM Channel Profiling

To check if the ETM channels are set with proper events and tasks, you can call `esp_etm_dump`{.interpreted-text role="cpp:func"} to dump all working ETM channels with their associated events and tasks. The dumping format is like:

    ===========ETM Dump Start==========
    channel 0: event 48 ==> task 17
    channel 1: event 48 ==> task 90
    channel 2: event 48 ==> task 94
    ===========ETM Dump End============

The digital ID printed in the dump information is defined in the `soc/soc_etm_source.h` file.

### Power Management {#etm-power-management}

When power management is enabled, i.e., `CONFIG_PM_ENABLE`{.interpreted-text role="ref"} is on, the system may adjust or disable the clock source, and power off the ETM peripheral before going to sleep. As a result, the existing connection between events and tasks will be lost, and the ETM channels can\'t work correctly after wake up. So by default, the driver will acquire a power management lock internally to forbid the system from powering off the ETM peripheral.

::: only
SOC_ETM_SUPPORT_SLEEP_RETENTION

If you want to save more power, you can set `esp_etm_channel_config_t::etm_chan_flags::allow_pd`{.interpreted-text role="cpp:member"} to `true`. Then ETM registers will be backed up before sleep and restored after wake up. Please note, enabling this option will increase the memory consumption for saving the register context.
:::

### Thread Safety {#etm-thread-safety}

The ETM core driver is thread-safe.

- Factory functions (for example, `esp_etm_new_channel`{.interpreted-text role="cpp:func"}, `gpio_new_etm_task`{.interpreted-text role="cpp:func"}, and other `*_new_etm_*` creators) are thread-safe and can be called from different RTOS tasks concurrently.
- Channel control APIs (for example, `esp_etm_channel_connect`{.interpreted-text role="cpp:func"}, `esp_etm_channel_enable`{.interpreted-text role="cpp:func"}, `esp_etm_channel_disable`{.interpreted-text role="cpp:func"}, `esp_etm_del_channel`{.interpreted-text role="cpp:func"}) are also thread-safe. Concurrent operations on different channels are safe. Concurrent operations on the same channel are serialized internally; if an operation is incompatible with the current channel state, it will return `ESP_ERR_INVALID_STATE`{.interpreted-text role="c:macro"}.
- No functions are allowed to run within the ISR environment.

### Kconfig Options {#etm-kconfig-options}

- `CONFIG_ETM_ENABLE_DEBUG_LOG`{.interpreted-text role="ref"} is used to enable the debug log output. Enabling this option increases the firmware binary size as well.

## API Reference

::: include-build-file
inc/esp_etm.inc
:::

::: include-build-file
inc/gpio_etm.inc
:::

::: include-build-file
inc/esp_systick_etm.inc
:::

[^1]: Different ESP chip series might have different numbers of ETM channels. For more details, please refer to *{IDF_TARGET_NAME} Technical Reference Manual* \> Chapter **Event Task Matrix (ETM)** \[[PDF]({IDF_TARGET_TRM_EN_URL}#evntaskmatrix)\]. The driver does not forbid you from applying for more channels, but it will return an error when all available hardware resources are used up. Please always check the return value when doing channel allocation (i.e., `esp_etm_new_channel`{.interpreted-text role="cpp:func"}).
