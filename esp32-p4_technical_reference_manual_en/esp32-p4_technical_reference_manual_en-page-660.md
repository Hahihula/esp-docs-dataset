

```markdown
Register 10.6. HP_SYS_CLKRST_SOC_CLK_CTRL0_REG (0x0014)

Continued from the previous page...

HP_SYS_CLKRST_L2MEMMON_SYS_CLK_EN Configures whether to enable the bus clock of L2 memory monitor (SYS_CLK clock domain).
O: Disable
1: Enable
(R/W)

HP_SYS_CLKRST_SPMMON_SYS_CLK_EN Configures whether to enable the bus clock of L2 SPM monitor (SYS_CLK clock domain).
O: Disable
1: Enable
(R/W)

HP_SYS_CLKRST_ICM_SYS_CLK_EN Configures the bus clock of the bus module (SYS_CLK clock domain).
O: Disable
1: Enable
(R/W)

HP_SYS_CLKRST_FLASH_SYS_CLK_EN Configures whether to enable the bus clock of flash (SYS_CLK clock domain).
O: Disable
1: Enable
(R/W)

HP_SYS_CLKRST_PSRAM_SYS_CLK_EN Configures whether to enable the bus clock of PSRAM (SYS_CLK clock domain).
O: Disable
1: Enable
(R/W)
```