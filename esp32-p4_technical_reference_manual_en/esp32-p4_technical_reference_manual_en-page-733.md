

```markdown
Register 10.51. HP_SYS_CLKRST_HP_FORCE_NORSTO_REG (0x00CC)

Continued from the previous page...

HP_SYS_CLKRST_FORCE_NORST_UART0 Configures whether UART0 can be reset.
    0: Can be reset
    1: Force not to be reset
    (R/W)

HP_SYS_CLKRST_FORCE_NORST_UART1 Configures whether UART1 can be reset.
    0: Can be reset
    1: Force not to be reset
    (R/W)

HP_SYS_CLKRST_FORCE_NORST_UART2 Configures whether UART2 can be reset.
    0: Can be reset
    1: Force not to be reset
    (R/W)

HP_SYS_CLKRST_FORCE_NORST_UART3 Configures whether UART3 can be reset.
    0: Can be reset
    1: Force not to be reset
    (R/W)

HP_SYS_CLKRST_FORCE_NORST_UART4 Configures whether UART4 can be reset.
    0: Can be reset
    1: Force not to be reset
    (R/W)

HP_SYS_CLKRST_FORCE_NORST_UHCI Configures whether UHCI can be reset.
    0: Can be reset
    1: Force not to be reset
    (R/W)

HP_SYS_CLKRST_FORCE_NORST_I3CMST Configures whether I3C Master can be reset.
    0: Can be reset
    1: Force not to be reset
    (R/W)
```