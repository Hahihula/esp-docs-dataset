

```markdown
Register 10.52. HP_SYS_CLKRST_HP_FORCE_NORST1_REG (0x00D0)

Continued from the previous page...

HP_SYS_CLKRST_FORCE_NORST_I2SO Configures whether I2SO can be reset.
    0: Can be reset
    1: Force not to be reset
    (R/W)

HP_SYS_CLKRST_FORCE_NORST_I2S1 Configures whether I2S1 can be reset.
    0: Can be reset
    1: Force not to be reset
    (R/W)

HP_SYS_CLKRST_FORCE_NORST_I2S2 Configures whether I2S2 can be reset.
    0: Can be reset
    1: Force not to be reset
    (R/W)

HP_SYS_CLKRST_FORCE_NORST_SPI2 Configures whether SPI2 can be reset.
    0: Can be reset
    1: Force not to be reset
    (R/W)

HP_SYS_CLKRST_FORCE_NORST_SPI3 Configures whether SPI3 can be reset.
    0: Can be reset
    1: Force not to be reset
    (R/W)

HP_SYS_CLKRST_FORCE_NORST_LCDCAM Configures whether the LCD_CAM can be reset.
    0: Can be reset
    1: Force not to be reset
    (R/W)

HP_SYS_CLKRST_FORCE_NORST_ADC Configures whether ADC can be reset.
    0: Can be reset
    1: Force not to be reset
    (R/W)

Continued on the next page...
```