

```markdown
Register 10.50. HP_SYS_CLKRST_HP_RST_EN2_REG (0x00C8)

Continued from the previous page...

HP_SYS_CLKRST_RST_EN_AES Configures whether to reset AES.
    0: Release from reset
    1: Reset
    (R/W)

HP_SYS_CLKRST_RST_EN_DS Configures whether to reset the RSA_DS peripheral.
    0: Release from reset
    1: Reset
    (R/W)

HP_SYS_CLKRST_RST_EN_SHA Configures whether to reset SHA.
    0: Release from reset
    1: Reset
    (R/W)

HP_SYS_CLKRST_RST_EN_HMAC Configures whether to reset HMAC.
    0: Release from reset
    1: Reset
    (R/W)

HP_SYS_CLKRST_RST_EN_ECDSA Configures whether to reset ECDSA_DS.
    0: Release from reset
    1: Reset
    (R/W)

HP_SYS_CLKRST_RST_EN_RSA Configures whether to reset RSA.
    0: Release from reset
    1: Reset
    (R/W)

HP_SYS_CLKRST_RST_EN_ECC Configures whether to reset ECC.
    0: Release from reset
    1: Reset
    (R/W)

HP_SYS_CLKRST_RST_EN_KM Configures whether to reset the Key Manager.
    0: Release from reset
    1: Reset
    (R/W)

HP_SYS_CLKRST_RST_EN_H264 Configures whether to reset H264.
    0: Release from reset
    1: Reset
    (R/W)
```