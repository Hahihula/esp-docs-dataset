

```markdown
Register 10.8. HP_SYS_CLKRST_SOC_CLK_CTRL2_REG (0x001C)

Continued from the previous page...

HP_SYS_CLKRST_UHCI_APB_CLK_EN Configures whether to enable the bus clock of the UHCI
(APB_CLK clock domain).
O: Disable
1: Enable
(R/W)

HP_SYS_CLKRST_UART0_APB_CLK_EN Configures whether to enable the bus clock of UART0
(APB_CLK clock domain).
O: Disable
1: Enable
(R/W)

HP_SYS_CLKRST_UART1_APB_CLK_EN Configures whether to enable the bus clock of UART1
(APB_CLK clock domain).
O: Disable
1: Enable
(R/W)

HP_SYS_CLKRST_UART2_APB_CLK_EN Configures whether to enable the bus clock of UART2
(APB_CLK clock domain).
O: Disable
1: Enable
(R/W)

HP_SYS_CLKRST_UART3_APB_CLK_EN Configures whether to enable the bus clock of UART3
(APB_CLK clock domain).
O: Disable
1: Enable
(R/W)

HP_SYS_CLKRST_UART4_APB_CLK_EN Configures whether to enable the bus clock of UART4
(APB_CLK clock domain).
O: Disable
1: Enable
(R/W)

HP_SYS_CLKRST_I2CO_APB_CLK_EN Configures whether to enable the bus clock of I2C0
(APB_CLK clock domain).
O: Disable
1: Enable
(R/W)

Continued on the next page...
```