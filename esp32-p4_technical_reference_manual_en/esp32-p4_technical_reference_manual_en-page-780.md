

```markdown
Chapter 12 Interrupt Matrix

mapping register for interrupt source RMT_INTR is COREx_RMT_INT_MAP_REG, and its status register is COREx_INTR_STATUS_REG_1_REG.

Note that COREx in the table can be CORE0 (HP CPU0) or CORE1 (HP CPU1).
```