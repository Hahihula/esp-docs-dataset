

```markdown
Chapter 58 Parallel IO Controller (PARLIO)

Register 58.21. PARL_IO_CLK_REG (0x0120)

PARL_IO_CLK_EN Register clock gating.

O: Enable the register clock only when reading or writing registers
1: Always enable the register clock
(R/W)
```

```markdown
Register 58.22. PARL_IO_VERSION_REG (0x03FC)

PARL_IO_DATE Version control register. (R/W)
```