

```markdown
| ID = Identifier | ACR = TWAI_ACCEPTANCE_CODE |
|-----------------|----------------------------|
| DB = Data Byte  | AMR = TWAI_ACCEPTANCE_MASK |

Figure 53.4-2. Single-Filter Mode

• EFF
    - The first 16 bits of the 29-bit ID
```