

```markdown
Chapter 10 Reset and Clock

Register 10.48. HP_SYS_CLKRST_HP_RST_ENO_REG (0x00C0)

Continued from the previous page...

HP_SYS_CLKRST_RST_EN_HP_CACHE Configures whether to reset L1 I-Cache0, L1 I-Cache1, L1 D-Cache, L2 Cache.
    0: Release from reset
    1: Reset
    (R/W)

HP_SYS_CLKRST_RST_EN_L1_IO_CACHE Configures whether to reset L1 I-Cache0.
    0: Release from reset
    1: Reset
    (R/W)

HP_SYS_CLKRST_RST_EN_L1_I_CACHE Configures whether to reset L1 I-Cache1.
    0: Release from reset
    1: Reset
    (R/W)

HP_SYS_CLKRST_RST_EN_L1_D_CACHE Configures whether to reset L1 D-Cache.
    0: Release from reset
    1: Reset
    (R/W)

HP_SYS_CLKRST_RST_EN_L2_CACHE Configures whether to reset L2 Cache.
    0: Release from reset
    1: Reset
    (R/W)

HP_SYS_CLKRST_RST_EN_L2_MEM Configures whether to reset L2 MEM.
    0: Release from reset
    1: Reset
    (R/W)

HP_SYS_CLKRST_RST_EN_L2MEMMON Configures whether to reset L2 MEM monitor.
    0: Release from reset
    1: Reset
    (R/W)

HP_SYS_CLKRST_RST_EN_SPMMON Configures whether to reset SPM monitor.
    0: Release from reset
    1: Reset
    (R/W)

HP_SYS_CLKRST_RST_EN_GDMA Configures whether to reset VDMA.
    0: Release from reset
    1: Reset
    (R/W)

Continued on the next page...
```