

```markdown\nChapter 39 H264 Encoder\nGoBack\n\nd) Wait for the H264_DB_TMP_READY_INT interrupt. When the interrupt is triggered, start H264_DMA TX channel 2. Refer to chapter 39.5.2.3 for the configuration.\ne) Wait for the H264_FRAME_DONE_INT interrupt. When the interrupt is triggered, it indicates that the picture encoding is completed.\nf) Reset H264_DMA TX channel 2 and RX channel 2: Refer to chapter 39.5.2.4 for the configuration.\ng) Reset the read and write time counter of deblocking filter intermediate data in H264_DMA: Set register H264_DMA_RX_CH2_INTER_COUNTER_RST to 1, then set to 0.\n\n- After any frame ends, the following registers can be reconfigured:\n  - Registers related to the quantization result decimate function. Refer to chapter 39.5.1.2 for details.\n  - Registers related to the MB level rate control function. Refer to chapter 39.5.1.4 for details.\n  - Deblocking filter enable register: H264_A_BYPASS_DB_FILTER.\n  - ROI function related registers. Refer to chapter 39.5.1.5 for details.\n  - Registers related to slice header information: H264_SLICE_RMRAIN_BIT, H264_SLICE_REMAIN_BITLENGTH, H264_SLICE_BYTE_LENGTH, H264_SLICE_BYTE_LSB, H264_SLICE_BYTE_MSB.\n  - Registers related to the MV merge function. Refer to chapter 39.5.1.3 for details.\n\n4. Repeat step 3 until all video frames in a GOP are encoded.\n5. Make sure the data is written completely. Refer to chapter 39.5.2.4 for the method.\n6. Reset each sending and receiving channel of H264_DMA referring to chapter 39.5.2.4.\n7. Reset the read and write times counter of deblocking filter result data, and the read and write times counter of reference picture data in H264_DMA. To be specific, set register H264_DMA_RX_CH0_EXTER_COUNTER_RST to 1, then set it to 0; Set register H264_DMA_RX_CH1_EXTER_COUNTER_RST to 1, then set it to 0; Set register H264_DMA_RX_CH5_INTER_COUNTER_RST to 1, then set it to 0.\n8. Reconfigure from step 2 and start encoding the next GOP.\n\n39.7.2 Dual Stream Mode\nIn the dual stream mode, two video sequences A and B are encoded. The encoding sequence of pictures is shown in Figure 39.7-2 (a GOP contains 3 pictures).\n\nFigure 39.7-2. Coding order of pictures in dual stream mode\n```
\n\n(Note: The diagram described as "Figure 39.7-2" shows a timeline with Stream A starting at an I frame (Frame 0), followed by P frames for both Stream A and Stream B across multiple GOPs, where each GOP contains three frames structured to encode pictures sequentially over time.)\n```