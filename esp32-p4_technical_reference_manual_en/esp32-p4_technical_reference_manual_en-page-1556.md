

```markdown
Chapter 34 Key Manager

k2 Auxiliary key used to assist with key deployment, applicable in both AES Deploy Mode and ECDH1 Deploy Mode. In AES Deployment Mode, its value does not affect the deployed key. In ECDH1 deployment mode, its value affects the negotiated deployment key.

k1_encrypted A key generated by encrypting k1 with k2. In AES Deploy Mode or ECDH1 Deploy Mode: k1_encrypted = AES{k1,k2}

k2_info The recovery information of k2. In AES Deploy Mode or ECDH1 Deploy Mode: k2_info = AES{AES{k2,rand_num}||rand_num,init_key}.

init_key Used in the generation of k2_info as well as in the recovery process from k2_info to k2. Its value can be stored in the eFuse block whose purpose is EFUSE_KM_INIT_KEY or provided through software configuration.

sw_init_key Software configured init_key

k1 * k2 * G A negotiated key generated by calculating k2 * G through ECC.

k1 * G A key generated by calculating k1 through ECC.

k2 * G An auxiliary public key generated by calculating k2 * G through ECC. This key is used later to generate k1 * k2 * G.

key_purpose The intended usage of the key.
```

## 34.4 Features

The HUK Generator has the following features:

- HUK Generation Mode:
    - Generates a new HUK and its recovery information
- HUK Recovery Mode:
    - Recovers a deployed HUK with its recovery information
- Prompt for HUK recovery error
- Prompt for HUK risk level

The Key Manager has the following features:

- Unlimited number of keys
- Specified private key deployment (AES Deploy Mode):
    - Users specify the value of the key
- Negotiated private key deployment (ECDHO Deploy Mode):
    - Highest security mode: there is no need to worry about the leaks of data in external channels
    - Requires chip initiation to obtain the private key
    - Negotiates the key value between each chip and the user
- Negotiated private key deployment (ECDH1 Deploy Mode):

Espressif Systems

Submit Documentation Feedback

ESP32-P4 TRM
PRELIMINARY
```