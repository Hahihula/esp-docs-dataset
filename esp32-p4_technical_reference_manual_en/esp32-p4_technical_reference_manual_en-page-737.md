

```markdown
Register 10.52. HP_SYS_CLKRST_HP_FORCE_NORST1_REG (0x00D0)

Continued from the previous page...

HP_SYS_CLKRST_FORCE_NORST_BITSRAMBLER Configures whether the Bit Scrambler can be reset.
    0: Can be reset
    1: Force not to be reset
    (R/W)

HP_SYS_CLKRST_FORCE_NORST_BITSRAMBLER_RX Configures whether the Bit Scrambler RX can be reset.
    0: Can be reset
    1: Force not to be reset
    (R/W)

HP_SYS_CLKRST_FORCE_NORST_BITSRAMBLER_TX Configures whether the Bit Scrambler TX can be reset.
    0: Can be reset
    1: Force not to be reset
    (R/W)

HP_SYS_CLKRST_FORCE_NORST_H264 Configures whether H264 can be reset.
    0: Can be reset
    1: Force not to be reset
    (R/W)
```