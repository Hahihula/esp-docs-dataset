

```markdown
Register 39.72. H264_DMA_OUT_PUSH_CH2_REG (0x0218)

H264_DMA_OUTFIFO_WDATA_CH2 Represents the data that needs to be pushed into DMA TX FIFO. (R/W)

H264_DMA_OUTFIFO_PUSH_CH2 Configures whether to push data into DMA TX FIFO.
    0: No effect
    1: Push
(R/W/SC)
```