

```markdown\n| Signal Name                | I/O | Description                                                                                                                                                                                                 |\n|----------------------------|-----|-------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------|\n| usb_otg_idpullup           | O   | Analog ID input Sample Enable. Enables sampling the analog ID line.<br>0: ID pin sampling disabled<br>1: ID pin sampling enabled                                                                       |\n| usb_otg_dppulldown         | O   | D+ Pull-down Resistor Enable. Enables the 15 kΩ pull-down resistor on the D+ line.                                                                                                                         |\n| usb_otg_dmpulldown         | O   | D- Pull-down Resistor Enable. Enables the 15 kΩ pull-down resistor on the D- line.                                                                                                                       |\n| usb_otg_drbbus             | O   | Drive Vbus. Enables driving Vbus to 5 V.<br>0: Do not drive Vbus<br>1: Drive Vbus                                                                                                                         |\n| usb_srp_chrgvbus           | O   | Vbus Input Charge Enable. Directs the PHY to charge Vbus.<br>0: Do not charge Vbus through a resistor<br>1: Charge Vbus through a resistor (must be active for at least 30 ms)                                       |\n| usb_srp_dischrgvbus        | O   | Vbus Input Discharge Enable. Directs the PHY to discharge Vbus.<br>0: Do not discharge Vbus through a resistor<br>1: Discharge Vbus through a resistor (must be active for at least 50 ms).                         |\n```\n\n## 50.5.2 ID Pin Detection\nBit `USB_CONIDSTS` in register `USB_GOTGCTL_REG` indicates whether the OTG controller is an A-device (`1'b0`) or a B-device (`1'b1`). The `USB_CONIDSTCHNG` interrupt will trigger whenever there is a change to `USB_CONIDSTS`, i.e., when a plug is connected or disconnected.\n\n## 50.5.3 Session Request Protocol (SRP)\n### 50.5.3.1 A-Device SRP\nFigure 50.5-1 illustrates the flow of SRP when the OTG_FS is acting as an A-device, i.e., default host and the device that powers Vbus.\n\n![Figure 50.5-1. A-Device SRP](image-placeholder)\n\n**Figure 50.5-1. A-Device SRP**\n\n1. To save power, the application suspends and turns off port power when the bus is idle by writing to the Port Suspend (`USB_PRTSUSP` to `1'b0`) and Port Power (`USB_PRTPWR` to `1'b0`) bits in the Host Port Control and Status register.\n```