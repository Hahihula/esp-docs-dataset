

```markdown
Register 6.13. DMA2D_OUT_COLOR_PARAM4_CHn_REG (n: 0-3) (0x0060+0x100*n)

DMA2D_OUT_COLOR_PARAM_LO_CHn   Configures the coefficient A and B for the least significant byte of the input of color space conversion’s second stage for TX channel n. (R/W)
```

```markdown
Register 6.14. DMA2D_OUT_COLOR_PARAM5_CHn_REG (n: 0-3) (0x0064+0x100*n)

DMA2D_OUT_COLOR_PARAM_L1_CHn   Configures the coefficient C and D for the least significant byte of the input of color space conversion’s second stage for TX channel n. (R/W)
```