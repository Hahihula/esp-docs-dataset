Title: Chapter 15 Permission Control (PMS)

Subtitle: Register 15.91. PMS_EDMA_PMS_RMT_REG (0x304)
- Diagram of a register with bits labeled from right to left as follows:
  - Bits are numbered starting at the far-right bit and going towards the center.
  - The label "Reset" is placed next to some specific group of bits on the diagram.

Body Text: 
PMS_EDMA_PMS_RMT_ATTR1 Configures Remote Control Peripheral's access to external SRAM Area0. For details, see Table 15.5-4. (R/W)

PMS_EDMA_PMS_RMT_ATTR2 Configures Remote Control Peripheral’s access to external SRAM Area1. For details, see Table 15.5-4. (R/W)

Subtitle: Register 15.92. PMS_CLOCK_GATE_REG_REG (0x308)
- Diagram of a register with bits labeled from right to left as follows:
  - Bits are numbered starting at the far-right bit and going towards the center.
  - The label "Reset" is placed next to some specific group of bits on the diagram.

Body Text: 
PMS_REG_CLK_EN Set this bit to enable the clock gating function. (R/W)

Footer Information:

- Left side vertical text:
  - ESP32-S3 TRM (Version 1.7)
  
- Right top corner label vertically aligned from bottom up, reading "Chapter 15 Permission Control (PMS)"
  
- Bottom left page number: 
  - Page 789
  
- Bottom right button labeled with an arrow pointing to the text:
  - GoBack

Note: The diagram includes binary numbers and labels indicating specific bits within a register. These are not transcribed as they represent graphical information rather than textual content that can be directly converted into markdown format without visual representation of their layout on paper or screen interface elements like buttons for navigation in PDF documents, etc., which would require additional context to interpret accurately from text alone here provided by the image description above this instruction.