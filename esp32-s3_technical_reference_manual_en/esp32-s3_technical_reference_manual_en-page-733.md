**Title:**
Register 15.24, PMS_DMA_APBPERI_LCD_CAM_PMS CONSTRAINT O_REG (0x0090)

**Diagram Description:**
- The diagram shows a bit map of the register.
- Bits are labeled as follows:
  - Bit 31 is marked as "reserved".
  - Other bits from positions 24 to 0 have labels such as PMS_DMA_APBPERI_LCD_CAM_PMS CONSTRAINT LOCK.

**Text Description:**
PMS_DMA_APBPERI_LCD_CAM_PMS CONSTRAINT_LOCK
Set this bit to lock LCD_CAM's GDMA permission configuration register. (R/W)

**Side Texts and Labels:**
- "Chapter 15 Permission Control (PMS)"
- On the left side, vertically aligned text reads:
  - Espressif Systems
  - Submit Documentation Feedback

- At the bottom of the page in a vertical orientation is labeled as:
  - ESP32-S3 TRM (Version 1.7)

**Additional Information:**
- The diagram includes binary values for each bit position, with "Reset" indicated at one end.
- There are navigation options like "GoBack".