**Title:**
Chapter 20 RSA Accelerator (RSA)

**Subtitles and Sections with Descriptions:**

1. **Register 20.12. RSA_INTERRUPT_ENA_REG (0x82C)**
   - Description:
     ```
     RSA_INTERRUPT_ENA
     (reserved)
     ```
   - Binary representation diagram for the register.
   - Explanation of the bit `RSA_INTERRUPT_ENA` with a default value set to 1.

2. **Register 20.13. RSA_DATE_REG (0x830)**
   - Description:
     ```
     RSA_DATE
     (reserved)
     ```
   - Binary representation diagram for the register.
   - Value `0x20190425` is shown.

3. **RSA_DATE Version control register. (R/W)**
   - No additional description provided, just a label indicating it's version control and can be read/written to.

**Footer:**
- "Espressif Systems"
- Page number `882`
- Document title or identifier: "ESP32-S3 TRM (Version 1.7)"
- Link for submitting documentation feedback

**Navigation Links:**
- GoBack button at the top right corner of each section.

**Binary Diagrams and Values:**
- Each register has a corresponding binary diagram showing bit positions.
- The RSA_INTERRUPT_ENA register shows bits from `31` to `0`.
- The RSA_DATE_REG register also displays its values in hexadecimal format.