**Title: Chapter 10 Low-power Management (RTC_CNTL)**

**Body Text:**
- signals from the main state machine.
- Sleep/wakeup controllers: send sleep or wakeup requests to the RTC main state machine.
- Clock controller: selects and powers up/down clock sources.

**Subtitle: Protection Timer:** 
- controls the transition interval between main state machine states.

In ESP32-S3’s power management unit, the sleep/wakeup controllers send sleep or wakeup requests to the RTC main state machine, which then generates power gating, clock gating, and reset signals. Then, the power controller and clock controller power up and power down different power domains and clock sources, according to the signals generated by the RTC main state machine, so that the chip enters or exits the low-power modes. The main workflow is shown in Figure 10.3-2.

**Figure Caption:**
- **Figure 10.3-2. Power Management Unit Workflow**

**Diagram Description (from top to bottom):**
- Clock Controller
- Power Controller
- Protection Timer

**RTC Main State Machine Diagram Components and Flow:**
- Sleep Controller -> ULP Timer -> ULP Coprocessor -> Touch Timer -> Touch Controller -> CPU wakeup
- Wakeup Sources:
  - Wakeup Source 1, ..., Wakeup Source n (represented as a series of boxes with ellipses indicating continuation)

**Note Section:**
1. For a complete list of power domains, please refer to Section 10.4.1.
2. For a complete list of all the available wakeup sources, please refer to Table 10.4-3.

**Footer Information:** 
- Espressif Systems
- Page Number: 569
- Document Version: ESP32-S3 TRM (Version 1.7)
- Submit Documentation Feedback