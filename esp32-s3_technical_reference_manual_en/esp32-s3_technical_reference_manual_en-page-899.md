**Chapter Title:**
Chapter 22 Digital Signature (DS)

**Body Text with Code and Explanation:**

- **[x]_s:** A bit string of s bits, in which s should be the integral multiple of 8 bits. If x is a number (x < 2^s), it is represented in little endian byte order in the bit string. x may be a variable value such as [Y]_{4096} or as a hexadecimal constant such as [0x0C]_s. If necessary, the value [x]_{t} can be right-padded with (s - t) number of 0 to reach s bits in length, and finally get [x]_s. For example, [0x05]_8 = 00000101, [0x05]_{16} = 000000001000000000, [0x0005]_{16} = 0000000000000000000101, [Ox13]_s = 00010011, [Ox13]_{16} = 0001001100000000.

- **||:** A bit string concatenation operator for joining multiple bit strings into a longer bit string.

**Subsection Title:**
22.3.2 Private Key Operands

**Body Text with Explanation and Details of RSA Operations:**

Private key operands Y (private key exponent) and M (key modulus) are generated by the user. They have a particular RSA key length (up to 4096 bits). Two additional private key operands are needed: τ and M'. These two operands are derived from Y and M.

Operands Y, M', τ and M' are encrypted by the user along with an authentication digest and stored as a single ciphertext C. C is inputted to the DS peripheral in this encrypted format, decrypted by the hardware, and then used for RSA signature calculation. Detailed description of how to generate C is provided in Section 22.3.3.

The DS peripheral supports RSA signature calculation Z = X^Y mod M, in which the length of operands should be N = 32 x where x ∈ {1, 2, 3,..., 128}. The bit lengths of arguments Z, X, Y, M and τ should be an arbitrary value in N, and all of them in a calculation must be of the same length, while the bit length of M' should always be 32. For more detailed information about RSA calculation, please refer to Section [20.3.1 Large Number Modular Exponentiation](#large-number-modular-exponentiation) in Chapter 20 RSA Accelerator (RSA).

**Subsection Title:**
22.3.3 Software Prerequisites

**Body Text with Explanation and Reference for Hardware Workflow:**

The left side of Figure [22.3-1](#figure-22.3-1) lists preparations required by the software before the hardware starts RSA signature calculation, while the right side lists the hardware workflow during the entire calculation procedure.

**Footer Information:**
Espressif Systems
Page Number 899 ESP32-S3 TRM (Version 1.7)
Submit Documentation Feedback