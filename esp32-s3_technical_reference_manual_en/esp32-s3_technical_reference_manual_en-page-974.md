**Title:**
Chapter 26 UART Controller (UART)

**Section Title and Description:**

- **Register 26.48. UHCI_REG_Q4_WORDO_REG (0x058)**
  - Text:
    ```
    UHCI_SEND_Q4_WORDO This register is used as a quick_sent register when mode is specified by
    UHCI_ALWAYS_SEND_NUM or UHCI_SINGLE_SEND_NUM. (R/W)
    ```

- **Register 26.49. UHCI_REG_Q4_WORD1_REG (0x05C)**
  - Text:
    ```
    UHCI_SEND_Q4_WORD1 This register is used as a quick_sent register when mode is specified by
    UHCI_ALWAYS_SEND_NUM or UHCI_SINGLE_SEND_NUM. (R/W)
    ```

- **Register 26.50. UHCI_REG_Q5_WORDO_REG (0x060)**
  - Text:
    ```
    UHCI_SEND_Q5_WORDO This register is used as a quick_sent register when mode is specified by
    UHCI_ALWAYS_SEND_NUM or UHCI_SINGLE_SEND_NUM. (R/W)
    ```

**Footer:**
Espressif Systems  
974 ESP32-S3 TRM (Version 1.7)  

**Link Texts:**
- GoBack

**Note:** The image contains diagrams with labels, but they are not described in detail as per the instructions to only describe them if understood or block diagrams/flowcharts.

**Page Number and Document Information at Bottom of Page:**
974 ESP32-S3 TRM (Version 1.7)  
Submit Documentation Feedback