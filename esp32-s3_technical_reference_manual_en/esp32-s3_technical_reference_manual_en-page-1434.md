**Title:**
Chapter 37 Remote Control Peripheral (RMT)

**Header:**
GoBack

**Diagram Description:**
A table with a binary representation of the RMT_INT_RAW_REG register, showing various interrupt events and their corresponding bits.

**Body Text:**

- **Section Title:** 
  - RMT_CHn_TX_END_INT_RAW (n = 0-3)
    - The interrupt raw bit of `RMT_CHn_TX_END_INT` (`R/WTC/SS`)

- **Section Title:** 
  - RMT_CHn_ERR_INT_RAW (n = 0-3)
    - The interrupt raw bit of `RMT_CHn_ERR_INT` (`R/WTC/SS`)

- **Section Title:**
  - RMT_CHn_TX_THR_EVENT_INT_RAW (n = 0-3)
    - The interrupt
      - raw bit

- **Section Title:** 
  - RMT_CHn_TX_LOOP_INT_RAW (n = 0-3)
    - The interrupt raw bit of `RMT_CHn_TX_LOOP_INT` (`R/WTC/SS`)

- **Section Title:**
  - RMT_CHm_RX_END_INT_RAW (m = 4-7)
    - The interrupt raw bit of `RMT_CHm_RX_END_INT` (`R/WTC/SS`)

- **Section Title:** 
  - RMT_CHm_ERR_INT_RAW (m = 4-7)
    - The interrupt raw bit of `RMT_CHm_ERR_INT` (`R/WTC/SS`)

- **Section Title:**
  - RMT_CHm_RX_THR_EVENT_INT_RAW (m = 4-7)
    - The interrupt
      - raw

- **Section Title:** 
  - RMT_CHm_RX_THR_EVENT_INT
    - `R/WTC/SS`

- **Section Title:** 
  - RMT_CH3_DMA_ACCESS_FAIL_INT_RAW
    - The interrupt
      - raw bit of `RMT_CH3_DMA_ACCESS_FAIL_INT` (`R/WTC/SS`)

- **Section Title:**
  - RMT_CH7_DMA_ACCESS_FAIL_INT_RAW
    - The interrupt
      - raw bit

**Footer Text:** 
Espressif Systems  
1434 ESP32-S3 TRM (Version 1.7)  

**Link:**
Submit Documentation Feedback