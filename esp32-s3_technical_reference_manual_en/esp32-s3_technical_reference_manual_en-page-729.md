**Title:**
Chapter 15 Permission Control (PMS)

**Header:**
Register 15.20. PMS_DMA_APBPERI_ADC_DAC_PMS CONSTRAINT_O_REG (0x0080)

**Body Text:**
- **Field Description:** 
  - `31` to `0`: Bits for various control functions.
  
- **Bit Description with Example Value:**
  - Bit at position `31` is labeled as `(reserved)`.
  - Bit at positions from `29` down to `0` are shown in binary format (e.g., `1`, `0`) indicating their current state.

**Field Label and Function:**
- **Label:** PMS_DMA_APBPERI_ADC_DAC_PMS CONSTRAINT_LOCK
- **Function Description:** Set this bit to lock ADC_DAC’s GDMA permission configuration register. (R/W)

**Footer Information:**
- Document version information on the left side:
  - "ESP32-S3 TRM (Version 1.7)"
  
- Navigation options at bottom right corner labeled as `GoBack`. 

**Side Texts and Labels:** 
- On both sides, there are labels indicating different sections or chapters of a document.
  - Left: Espressif Systems
  - Right Top Corner: Chapter 15 Permission Control (PMS)
  
- Bottom left side:
  - "Submit Documentation Feedback" with an icon that appears to be for feedback submission.