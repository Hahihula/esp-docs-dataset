**Title:**
Chapter 36 Motor Control PWM (MCPWM)

**Subtitle:**
Register 36.35. MCPWM_GEN1_A_REG (0x0088)

**Body Text with Table and Descriptions of Registers:**

- **MCPWM_GEN1_A_UTEZ**: Action on PWM1A triggered by event TEZ when timer increasing.
  - Description:
    ```
    change, 1: low, 2: high, 3: toggle. (R/W)
    ```

- **MCPWM_GEN1_A_UTEP**: Action on PWM1A triggered by event TEP when timer increasing.
  - Description:
    ```
    change, 1: low, 2: high, 3: toggle. (R/W)
    ```

- **MCPWM_GEN1_A_UTEA**: Action on PWM1A triggered by event TEA when timer increasing.
  - Description:
    ```
    change, 1: low, 2: high, 3: toggle. (R/W)
    ```

- **MCPWM_GEN1_A_UTEB**: Action on PWM1A triggered by event TEB when timer increasing.
  - Description:
    ```
    change, 1: low, 2: high, 3: toggle. (R/W)
    ```

- **MCPWM_GEN1_AUTO**: Action on PWM1A triggered by event_t0 when timer increasing.
  - Description:
    ```
    change, 1: low, 2: high, 3: toggle. (R/W)
    ```

- **MCPWM_GEN1 AUT1**: Action on PWM1A triggered by event_t1 when timer increasing.
  - Description:
    ```
    change, 1: low, 2: high, 3: toggle. (R/W)
    ```

- **MCPWM_GEN1 A_DTEZ**: Action on PWM1A triggered by event TEZ when timer decreasing.
  - Description:
    ```
    change, 1: low, 2: high, 3: toggle. (R/W)
    ```

- **MCPWM_GEN1 A_DTEP**: Action on PWM1A triggered by event TEP when timer decreasing.
  - Description:
    ```
    change, 1: low, 2: high, 3: toggle. (R/W)
    ```

- **MCPWM_GEN1 A_DTEA**: Action on PWM1A triggered by event TEA when timer decreasing.
  - Description:
    ```
    change, 1: low, 2: high, 3: toggle. (R/W)
    ```

- **MCPWM_GEN1 A_DTEB**: Action on PWM1A triggered by event TEB when timer decreasing.
  - Description:
    ```
    change, 1: low, 2: high, 3: toggle. (R/W)
    ```

- **MCPWM_GEN1 A_DT0**: Action on PWM1A triggered by event_t0 when timer decreasing.
  - Description:
    ```
    change, 1: low, 2: high, 3: toggle. (R/W)
    ```

- **MCPWM_GEN1 A_DT1**: Action on PWM1A triggered by event_t1 when timer decreasing.
  - Description:
    ```
    change, 1: low, 2: high, 3: toggle. (R/W)
    ```

**Footer Information:**
Espressif Systems
ESP32-S3 TRM (Version 1.7)

**Link:**
Submit Documentation Feedback