**Title:**
Chapter 15 Permission Control (PMS)

**Register Information:**
- **Register Name:** PMS_CORE_X_DRAMO_PMS CONSTRAINT O_REG (0x00FC)
- **Description:** Set this bit to lock the permission of CPU DBUS to internal SRAM. (R/W)

**Bit Description Table:**
- The table shows a binary representation with 32 bits.
- Specific positions are labeled:
  - Bit position `31` is marked as `(reserved)`
  - Bits from `0` through `30` have labels indicating their function, which in this case seems to be all zeros.

**Bit Labels:**
- The bit at the far right (bit `0`) has a label "Reset".

**Side Texts and Navigation:**
- On the left side vertically aligned text reads:
  - Espressif Systems
  - Submit Documentation Feedback

- At the bottom, there is additional information indicating this document pertains to ESP32-S3 TRM (Version 1.7).

- There are navigation options at the top right corner labeled "GoBack".