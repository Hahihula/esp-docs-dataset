

```markdown
Register 10.5. SYSTIMER_UNITO_VALUE_HI_REG (0x0040)
```

SYSTIMER_TIMER_UNITO_VALUE_HI UNITO read value, high 20 bits. (RO)

```markdown
Register 10.6. SYSTIMER_UNITO_VALUE_LO_REG (0x0044)
```

SYSTIMER_TIMER_UNITO_VALUE_LO UNITO read value, low 32 bits. (RO)

```markdown
Register 10.7. SYSTIMER_UNITO_LOAD_REG (0x005C)
```

SYSTIMER_TIMER_UNITO_LOAD UNITO synchronization enable signal. Set this bit to reload the values of SYSTIMER_TIMER_UNITO_LOAD_HI and SYSTIMER_TIMER_UNITO_LOAD_LO to UNITO. (WT)
```