

```markdown
Chapter 33 SPI Controller (SPI)

Register 33.23. SPI_W1_REG (0x009C)
```
![Register 33.23: SPI_W1_REG bit field diagram showing a single row labeled "SPI_BUF1" with bits from 31 to 0, reset value of 0.]

```markdown
SPI_BUF1 32-bit data buffer 1.
(R/W/SS)

Register 33.24. SPI_W2_REG (0x00A0)
```
![Register 33.24: SPI_W2_REG bit field diagram showing a single row labeled "SPI_BUF2" with bits from 31 to 0, reset value of 0.]

```markdown
SPI_BUF2 32-bit data buffer 2.
(R/W/SS)

Register 33.25. SPI_W3_REG (0x00A4)
```
![Register 33.25: SPI_W3_REG bit field diagram showing a single row labeled "SPI_BUF3" with bits from 31 to 0, reset value of 0.]

```markdown
SPI_BUF3 32-bit data buffer 3.
(R/W/SS)

Register 33.26. SPI_W4_REG (0x00A8)
```
![Register 33.26: SPI_W4_REG bit field diagram showing a single row labeled "SPI_BUF4" with bits from 31 to 0, reset value of 0.]

```markdown
SPI_BUF4 32-bit data buffer 4. (R/W/SS)
```

Espressif Systems

Submit Documentation Feedback

ESP32-C5 TRM (Version 1.0)

1193
```