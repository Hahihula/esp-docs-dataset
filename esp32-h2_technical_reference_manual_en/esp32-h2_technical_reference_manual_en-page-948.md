

```markdown
Chapter 32 Pulse Count Controller (PCNT)

Register 32.7. PCNT_INT_RAW_REG (0x0040)
```

![Register 32.7 Diagram](image_path_if_available)  
*(Note: No actual image data provided, but the diagram would typically show a register bitfield layout with labels for each bit position and description of reserved/used bits.)*

```markdown
PCNT_CNT_THR_EVENT_Un_INT_RAW The raw interrupt status of the PCNT_CNT_THR_EVENT_Un_INT interrupt. (RO)
```

Register 32.8. PCNT_INT_ST_REG (0x0044)

![Register 32.8 Diagram](image_path_if_available)  
*(Note: No actual image data provided, but the diagram would typically show a register bitfield layout with labels for each bit position and description of reserved/used bits.)*

```markdown
PCNT_CNT_THR_EVENT_Un_INT_ST The masked interrupt status of the PCNT_CNT_THR_EVENT_Un_INT interrupt. (RO)
```

Register 32.9. PCNT_INT_ENA_REG (0x0048)

![Register 32.9 Diagram](image_path_if_available)  
*(Note: No actual image data provided, but the diagram would typically show a register bitfield layout with labels for each bit position and description of reserved/used bits.)*

```markdown
PCNT_CNT_THR_EVENT_Un_INT_ENA Write 1 to enable the PCNT_CNT_THR_EVENT_Un_INT interrupt. (R/W)
```

Espressif Systems  
948  
ESP32-H2 TRM (Version 1.1)  

Submit Documentation Feedback
```