

```markdown
Chapter 26 SPI Controller (SPI)

Register 26.31. SPI_W9_REG (0x00BC)
```

![Register diagram for SPI_W9_REG with label "SPI_BUF9" pointing to the top line]

```markdown
SPI_BUF9 32-bit data buffer 9.
(R/W/SS)
```

```markdown
Register 26.32. SPI_W10_REG (0x00C0)
```

![Register diagram for SPI_W10_REG with label "SPI_BUF10" pointing to the top line]

```markdown
SPI_BUF10 32-bit data buffer 10.
(R/W/SS)
```

```markdown
Register 26.33. SPI_W11_REG (0x00C4)
```

![Register diagram for SPI_W11_REG with label "SPI_BUF11" pointing to the top line]

```markdown
SPI_BUF11 32-bit data buffer 11.
(R/W/SS)
```

```markdown
Register 26.34. SPI_W12_REG (0x00C8)
```

![Register diagram for SPI_W12_REG with label "SPI_BUF12" pointing to the top line]

```markdown
SPI_BUF12 32-bit data buffer 12.
(R/W/SS)
```

Espressif Systems

961

ESP32-C61 TRM (Pre-release v0.5)

Submit Documentation Feedback PRELIMINARY
```