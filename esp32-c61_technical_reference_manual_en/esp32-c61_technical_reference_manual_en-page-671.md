

```markdown\n| Value | Source                                                                 |\n|-------|------------------------------------------------------------------------|\n| 4     | Reserved                                                              |\n| 5     | TCM_MEM_MONITOR                                                       |\n| 6     | TRACE                                                                |\n| 7     | Reserved                                                              |\n| 8     | PSRAM_MEM_MONITOR                                                     |\n| 9 ~ 15| Reserved                                                              |\n| 16 ~ 31| See the peripherals corresponding to the values 0 ~ 15 in Chapter 3 GDMA Controller (GDMA) > Table 3.4-1 GDMA Selecting Peripherals via Register Configuration. For example, 16 corresponds to the peripheral with value 0 in Table 3.4-1, and 17 corresponds to the peripheral with value 1 in the table, and so on. |\n```\n\nConfigure `TEE_Mn_LOCK` to lock the configuration of the master’s secure mode. Only chip reset, system reset, and core reset can unlock the configurations and restore all configurations in TEE registers to their default values.\n\n## 16.5.2 APM Controller Functional Description\n### 16.5.2.1 Architecture\nFigure 16.5-1 shows the architecture of the APM controller and the access paths managed by it.\n\n![Figure 16.5-1. APM Controller Architecture](#)\n\nAs shown in the figure, there are three APM controllers: `HP_APM_CTRL`, `LP_APM_CTRL`, and `CPU_APM_CTRL`, configured by their register modules `HP_APM_REG`, `LP_APM_REG`, and `CPU_APM_REG`, respectively.\n\n- `HP_APM_CTRL` manages four access paths, namely M0–M3 highlighted in blue in Figure 16.5-1. Enable the permission management of each path by setting `HP_APM_FUNC_CTRL_REG` to 1 (enabled by default).\n- `LP_APM_CTRL` manages one access path, namely M0 highlighted in orange in Figure 16.5-1. Enable the permission management of each path by setting `LP_APM_FUNC_CTRL_REG` to 1 (enabled by default).\n```