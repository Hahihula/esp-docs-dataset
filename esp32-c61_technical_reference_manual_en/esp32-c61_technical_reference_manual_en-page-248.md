

```markdown
Register 5.38. EFUSE_WR_TIM_CONF0_RS_BYPASS_REG (0x01F8)
```

| Bit Range | Field Name                          | Description                                                                 |
|-----------|-------------------------------------|-----------------------------------------------------------------------------|
| 31        | (reserved)                         |                                                                             |
| 21-20     |                                     |                                                                             |
| 13-12     | EFUSE_TPGM_INACTIVE                | Configures the inactive programming time. Measurement unit: One cycle of the eFuse core clock. (R/W) |
| 11        | EFUSE_UPDATE                       | Configures whether to update multi-bit register signals.<br>1: Update<br>0: No effect (WT) |
| 1-0       | EFUSE_BYPASS_RS_BLK_NUM            | Configures block number for the double programming operation. (R/W)          |

```markdown
EFUSE_BYPASS_RS_CORRECTION Configures whether to bypass Reed-Solomon correction step.
0: Not bypass<br>
1: Bypass<br>
(R/W)
```

```markdown
EFUSE_TPGM_INACTIVE Configures the inactive programming time.<br>Measurement unit: One cycle of the eFuse core clock. (R/W)
```
```markdown
EFUSE_UPDATE Configures whether to update multi-bit register signals.
1: Update
0: No effect
(WT)
```

```markdown
EFUSE_BYPASS_RS_BLK_NUM Configures block number for the double programming operation.
(R/W)
```

```markdown
EFUSE_BYPASS_RS_CORRECTION Configures whether to bypass Reed-Solomon correction step.

0: Not bypass

1: Bypass

(R/W)
```
```markdown
EFUSE_TPGM_INACTIVE Configures the inactive programming time.

Measurement unit: One cycle of the eFuse core clock. (R/W)
```

```markdown
EFUSE_UPDATE Configures whether to update multi-bit register signals.

1: Update

0: No effect

(WT)
```
```markdown
EFUSE_BYPASS_RS_BLK_NUM Configures block number for the double programming operation.

(R/W)
```
```markdown
EFUSE_BYPASS_RS_CORRECTION Configures whether to bypass Reed-Solomon correction step.

0: Not bypass

1: Bypass

(R/W)
```

```markdown
EFUSE_TPGM_INACTIVE Configures the inactive programming time.

Measurement unit: One cycle of the eFuse core clock. (R/W)
```
```markdown
EFUSE_UPDATE Configures whether to update multi-bit register signals.

1: Update

0: No effect

(WT)
```

```markdown
EFUSE_BYPASS_RS_BLK_NUM Configures block number for the double programming operation.

(R/W)
```
```markdown
EFUSE_BYPASS_RS_CORRECTION Configures whether to bypass Reed-Solomon correction step.

0: Not bypass

1: Bypass

(R/W)
```

```markdown
EFUSE_TPGM_INACTIVE Configures the inactive programming time.

Measurement unit: One cycle of the eFuse core clock. (R/W)
```
```markdown
EFUSE_UPDATE Configures whether to update multi-bit register signals.

1: Update

0: No effect

(WT)
```

```markdown
EFUSE_BYPASS_RS_BLK_NUM Configures block number for the double programming operation.

(R/W)
```
```markdown
EFUSE_BYPASS_RS_CORRECTION Configures whether to bypass Reed-Solomon correction step.

0: Not bypass

1: Bypass

(R/W)
```

```markdown
EFUSE_TPGM_INACTIVE Configures the inactive programming time.

Measurement unit: One cycle of the eFuse core clock. (R/W)
```
```markdown
EFUSE_UPDATE Configures whether to update multi-bit register signals.

1: Update

0: No effect

(WT)
```

```markdown
EFUSE_BYPASS_RS_BLK_NUM Configures block number for the double programming operation.

(R/W)
```
```markdown
EFUSE_BYPASS_RS_CORRECTION Configures whether to bypass Reed-Solomon correction step.

0: Not bypass

1: Bypass

(R/W)
```

```markdown
EFUSE_TPGM_INACTIVE Configures the inactive programming time.

Measurement unit: One cycle of the eFuse core clock. (R/W)
```
```markdown
EFUSE_UPDATE Configures whether to update multi-bit register signals.

1: Update

0: No effect

(WT)
```

```markdown
EFUSE_BYPASS_RS_BLK_NUM Configures block number for the double programming operation.

(R/W)
```
```markdown
EFUSE_BYPASS_RS_CORRECTION Configures whether to bypass Reed-Solomon correction step.

0: Not bypass

1: Bypass

(R/W)
```

```markdown
EFUSE_TPGM_INACTIVE Configures the inactive programming time.

Measurement unit: One cycle of the eFuse core clock. (R/W)
```
```markdown
EFUSE_UPDATE Configures whether to update multi-bit register signals.

1: Update

0: No effect

(WT)
```

```markdown
EFUSE_BYPASS_RS_BLK_NUM Configures block number for the double programming operation.

(R/W)
```
```markdown
EFUSE_BYPASS_RS_CORRECTION Configures whether to bypass Reed-Solomon correction step.

0: Not bypass

1: Bypass

(R/W)
```

```markdown
EFUSE_TPGM_INACTIVE Configures the inactive programming time.

Measurement unit: One cycle of the eFuse core clock. (R/W)
```
```markdown
EFUSE_UPDATE Configures whether to update multi-bit register signals.

1: Update

0: No effect

(WT)
```

```markdown
EFUSE_BYPASS_RS_BLK_NUM Configures block number for the double programming operation.

(R/W)
```
```markdown
EFUSE_BYPASS_RS_CORRECTION Configures whether to bypass Reed-Solomon correction step.

0: Not bypass

1: Bypass

(R/W)
```

```markdown
EFUSE_TPGM_INACTIVE Configures the inactive programming time.

Measurement unit: One cycle of the eFuse core clock. (R/W)
```
```markdown
EFUSE_UPDATE Configures whether to update multi-bit register signals.

1: Update

0: No effect

(WT)
```

```markdown
EFUSE_BYPASS_RS_BLK_NUM Configures block number for the double programming operation.

(R/W)
```
```markdown
EFUSE_BYPASS_RS_CORRECTION Configures whether to bypass Reed-Solomon correction step.

0: Not bypass

1: Bypass

(R/W)
```

```markdown
EFUSE_TPGM_INACTIVE Configures the inactive programming time.

Measurement unit: One cycle of the eFuse core clock. (R/W)
```
```markdown
EFUSE_UPDATE Configures whether to update multi-bit register signals.

1: Update

0: No effect

(WT)
```

```markdown
EFUSE_BYPASS_RS_BLK_NUM Configures block number for the double programming operation.

(R/W)
```
```markdown
EFUSE_BYPASS_RS_CORRECTION Configures whether to bypass Reed-Solomon correction step.

0: Not bypass

1: Bypass

(R/W)
```

```markdown
EFUSE_TPGM_INACTIVE Configures the inactive programming time.

Measurement unit: One cycle of the eFuse core clock. (R/W)
```
```markdown
EFUSE_UPDATE Configures whether to update multi-bit register signals.

1: Update

0: No effect

(WT)
```

```markdown
EFUSE_BYPASS_RS_BLK_NUM Configures block number for the double programming operation.

(R/W)
```
```markdown
EFUSE_BYPASS_RS_CORRECTION Configures whether to bypass Reed-Solomon correction step.

0: Not bypass

1: Bypass

(R/W)
```

```markdown
EFUSE_TPGM_INACTIVE Configures the inactive programming time.

Measurement unit: One cycle of the eFuse core clock. (R/W)
```
```markdown
EFUSE_UPDATE Configures whether to update multi-bit register signals.

1: Update

0: No effect

(WT)
```

```markdown
EFUSE_BYPASS_RS_BLK_NUM Configures block number for the double programming operation.

(R/W)
```
```markdown
EFUSE_BYPASS_RS_CORRECTION Configures whether to bypass Reed-Solomon correction step.

0: Not bypass

1: Bypass

(R/W)
```

```markdown
EFUSE_TPGM_INACTIVE Configures the inactive programming time.

Measurement unit: One cycle of the eFuse core clock. (R/W)
```
```markdown
EFUSE_UPDATE Configures whether to update multi-bit register signals.

1: Update

0: No effect

(WT)
```

```markdown
EFUSE_BYPASS_RS_BLK_NUM Configures block number for the double programming operation.

(R/W)
```
```markdown
EFUSE_BYPASS_RS_CORRECTION Configures whether to bypass Reed-Solomon correction step.

0: Not bypass

1: Bypass

(R/W)
```

```markdown
EFUSE_TPGM_INACTIVE Configures the inactive programming time.

Measurement unit: One cycle of the eFuse core clock. (R/W)
```
```markdown
EFUSE_UPDATE Configures whether to update multi-bit register signals.

1: Update

0: No effect

(WT)
```

```markdown
EFUSE_BYPASS_RS_BLK_NUM Configures block number for the double programming operation.

(R/W)
```
```markdown
EFUSE_BYPASS_RS_CORRECTION Configures whether to bypass Reed-Solomon correction step.

0: Not bypass

1: Bypass

(R/W)
```

```markdown
EFUSE_TPGM_INACTIVE Configures the inactive programming time.

Measurement unit: One cycle of the eFuse core clock. (R/W)
```
```markdown
EFUSE_UPDATE Configures whether to update multi-bit register signals.

1: Update

0: No effect

(WT)
```

```markdown
EFUSE_BYPASS_RS_BLK_NUM Configures block number for the double programming operation.

(R/W)
```
```markdown
EFUSE_BYPASS_RS_CORRECTION Configures whether to bypass Reed-Solomon correction step.

0: Not bypass

1: Bypass

(R/W)
```

```markdown
EFUSE_TPGM_INACTIVE Configures the inactive programming time.

Measurement unit: One cycle of the eFuse core clock. (R/W)
```
```markdown
EFUSE_UPDATE Configures whether to update multi-bit register signals.

1: Update

0: No effect

(WT)
```

```markdown
EFUSE_BYPASS_RS_BLK_NUM Configures block number for the double programming operation.

(R/W)
```
```markdown
EFUSE_BYPASS_RS_CORRECTION Configures whether to bypass Reed-Solomon correction step.

0: Not bypass

1: Bypass

(R/W)
```

```markdown
EFUSE_TPGM_INACTIVE Configures the inactive programming time.

Measurement unit: One cycle of the eFuse core clock. (R/W)
```
```markdown
EFUSE_UPDATE Configures whether to update multi-bit register signals.

1: Update

0: No effect

(WT)
```

```markdown
EFUSE_BYPASS_RS_BLK_NUM Configures block number for the double programming operation.

(R/W)
```
```markdown
EFUSE_BYPASS_RS_CORRECTION Configures whether to bypass Reed-Solomon correction step.

0: Not bypass

1: Bypass

(R/W)
```

```markdown
EFUSE_TPGM_INACTIVE Configures the inactive programming time.

Measurement unit: One cycle of the eFuse core clock. (R/W)
```
```markdown
EFUSE_UPDATE Configures whether to update multi-bit register signals.

1: Update

0: No effect

(WT)
```

```markdown
EFUSE_BYPASS_RS_BLK_NUM Configures block number for the double programming operation.

(R/W)
```
```markdown
EFUSE_BYPASS_RS_CORRECTION Configures whether to bypass Reed-Solomon correction step.

0: Not bypass

1: Bypass

(R/W)
```

```markdown
EFUSE_TPGM_INACTIVE Configures the inactive programming time.

Measurement unit: One cycle of the eFuse core clock. (R/W)
```
```markdown
EFUSE_UPDATE Configures whether to update multi-bit register signals.

1: Update

0: No effect

(WT)
```

```markdown
EFUSE_BYPASS_RS_BLK_NUM Configures block number for the double programming operation.

(R/W)
```
```markdown
EFUSE_BYPASS_RS_CORRECTION Configures whether to bypass Reed-Solomon correction step.

0: Not bypass

1: Bypass

(R/W)
```

```markdown
EFUSE_TPGM_INACTIVE Configures the inactive programming time.

Measurement unit: One cycle of the eFuse core clock. (R/W)
```
```markdown
EFUSE_UPDATE Configures whether to update multi-bit register signals.

1: Update

0: No effect

(WT)
```

```markdown
EFUSE_BYPASS_RS_BLK_NUM Configures block number for the double programming operation.

(R/W)
```
```markdown
EFUSE_BYPASS_RS_CORRECTION Configures whether to bypass Reed-Solomon correction step.

0: Not bypass

1: Bypass

(R/W)
```

```markdown
EFUSE_TPGM_INACTIVE Configures the inactive programming time.

Measurement unit: One cycle of the eFuse core clock. (R/W)
```
```markdown
EFUSE_UPDATE Configures whether to update multi-bit register signals.

1: Update

0: No effect

(WT)
```

```markdown
EFUSE_BYPASS_RS_BLK_NUM Configures block number for the double programming operation.

(R/W)
```
```markdown
EFUSE_BYPASS_RS_CORRECTION Configures whether to bypass Reed-Solomon correction step.

0: Not bypass

1: Bypass

(R/W)
```

```markdown
EFUSE_TPGM_INACTIVE Configures the inactive programming time.

Measurement unit: One cycle of the eFuse core clock. (R/W)
```
```markdown
EFUSE_UPDATE Configures whether to update multi-bit register signals.

1: Update

0: No effect

(WT)
```

```markdown
EFUSE_BYPASS_RS_BLK_NUM Configures block number for the double programming operation.

(R/W)
```
```markdown
EFUSE_BYPASS_RS_CORRECTION Configures whether to bypass Reed-Solomon correction step.

0: Not bypass

1: Bypass

(R/W)
```

```markdown
EFUSE_TPGM_INACTIVE Configures the inactive programming time.

Measurement unit: One cycle of the eFuse core clock. (R/W)
```
```markdown
EFUSE_UPDATE Configures whether to update multi-bit register signals.

1: Update

0: No effect

(WT)
```

```markdown
EFUSE_BYPASS_RS_BLK_NUM Configures block number for the double programming operation.

(R/W)
```
```markdown
EFUSE_BYPASS_RS_CORRECTION Configures whether to bypass Reed-Solomon correction step.

0: Not bypass

1: Bypass

(R/W)
```

```markdown
EFUSE_TPGM_INACTIVE Configures the inactive programming time.

Measurement unit: One cycle of the eFuse core clock. (R/W)
```
```markdown
EFUSE_UPDATE Configures whether to update multi-bit register signals.

1: Update

0: No effect

(WT)
```

```markdown
EFUSE_BYPASS_RS_BLK_NUM Configures block number for the double programming operation.

(R/W)
```
```markdown
EFUSE_BYPASS_RS_CORRECTION Configures whether to bypass Reed-Solomon correction step.

0: Not bypass

1: Bypass

(R/W)
```

```markdown
EFUSE